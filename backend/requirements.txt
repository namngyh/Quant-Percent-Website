# Lower bounds rather than exact pins: the Docker image builds on Python
# 3.12 while developers may be on 3.13/3.14, and these are the first
# releases shipping wheels for all three.
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sqlalchemy[asyncio]>=2.0.51
asyncpg>=0.31.0
alembic>=1.14.0
pydantic>=2.12.0
pydantic-settings>=2.7.0
# app/services/portfolio.py does the covariance work in numpy. It reached
# requirements.lock without ever being declared here, so the image built and
# every fresh dev environment failed on import.
numpy>=2.3.0
email-validator>=2.2.0
argon2-cffi>=23.1.0
pyjwt>=2.10.1
redis>=5.2.1
httpx>=0.28.1
apscheduler>=3.11.0
