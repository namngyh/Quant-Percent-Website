seed: 42
seeds: [42, 123, 2026, 3407, 7777]
device: cpu
deterministic: true
horizons: [5, 20, 60]
quantiles: [0.05, 0.25, 0.50, 0.75, 0.95]
mdd_quantiles: [0.10, 0.50, 0.90]
lookback: 378
purge_gap: 60
calibration_window: 252
target_coverage: 0.90
min_feature_nonmissing: 0.80
split: {development: 0.70, calibration: 0.15, test: 0.15, min_development: 1500, min_calibration: 252, min_test: 252}
model: {hidden_dim: 64, latent_dim: 48, n_blocks: 3, dropout: 0.10, gate_temperature: 1.0, medium_pool: 5, long_pool: 10}
training: {epochs: 150, tuning_epochs: 100, patience: 12, batch_size: 64, learning_rate: 0.001, weight_decay: 0.00001, folds: 5, trials: 80, num_workers: 0, seeds: [42, 123, 2026, 3407, 7777]}
tuning: {enabled: true, n_trials: 80, n_folds: 5, max_epochs: 100, early_stopping_patience: 12, pruner: hyperband, seed: 42, n_jobs: 1, storage: artifacts/tuning/msdp_optuna.db}
loss_weights: {return: 1.0, direction: 0.25, mdd: 0.35, volatility: 0.15, balance: 0.01, aux: 0.05, entropy: 0.0}
bootstrap_resamples: 2000
bootstrap: {n_resamples: 2000}
ablations: [equal, single, no_range, no_conformal, shared_gate]
run_label: full
data_quality: {invalid_ohlc_policy: mask_features}
