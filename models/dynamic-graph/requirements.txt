# Core (required for the statistical DynamicGraph pipeline + baselines)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
networkx>=3.0
pyyaml>=6.0
pydantic>=2.0
typer>=0.9
rich>=13.0
joblib>=1.3
matplotlib>=3.7
tabulate>=0.9          # markdown tables in the generated reports

# Storage / IO (recommended)
pyarrow>=12.0

# API (optional; `pip install -e .[api]`)
fastapi>=0.110
uvicorn>=0.27

# Tuning (optional)
optuna>=3.5

# Dev
pytest>=7.4

# ---------------------------------------------------------------------------
# OPTIONAL extras that the core pipeline degrades gracefully without:
#   duckdb, sqlalchemy         -> extra database backends
#   plotly                     -> interactive figures
#   shap, interpret            -> explainability / EBM
#   xgboost, lightgbm          -> extra gradient boosting baselines
#   python-louvain, leidenalg  -> better community detection
#   torch, torch-geometric     -> Temporal GNN (Phase 5)
# ---------------------------------------------------------------------------
